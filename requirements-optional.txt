# Optional runtime dependencies for simulation, NetCDF normalization, and visualization.
# Keep NumPy below 2.5 until the pinned/installed Numba release supports it.
numpy>=1.22,<2.5
numba
netCDF4
cdflib  # NASA CDF decoding for ISEE Japan VLF archive
h5py
zarr
pyvista

# Optional CPU neural baseline. Install a CPU build in the project venv.
torch
